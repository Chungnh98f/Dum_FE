import styled from "styled-components";

export const CamsContainer = styled.div`
  & .cam-list {
    display: flex;
    justify-content: space-around;
    flex-wrap: wrap;
  }
`;

export const StyledVideo = styled.video`
  height: 40%;
  width: 50%;
`;